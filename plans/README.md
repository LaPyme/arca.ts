# Implementation Plans

Generated by the improve skill on 2026-09-02. Execute the plan below in its
documented pull-request and release order. Read the complete plan before
starting, honor every STOP condition, and update the status row after each
milestone.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Deliver the v0.6.1 safety patch and v0.7.0 DX contract | P1 | L | — | TODO |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED: <reason>`, or
`REJECTED: <reason>`.

## Dependency notes

- Plan 001 is one release program with five implementation PRs. The release
  integrity PR must land before any package release.
- The v0.6.1 release must complete before the v0.7.0 release PR is merged.
- The v0.7.0 diagnostic-safety foundation must land before authentication
  classification, because authentication failures must use the safe error
  metadata contract.
- The deterministic decimal layer must land before the high-level Factura B
  and Factura C builders.
- Only maintainers may merge release PRs or publish packages. Completing the
  implementation plan does not grant release authority.

## Findings considered and rejected or deferred

- Deprecating `createNextVoucher()`: rejected. Keep it as the shortest
  single-writer path and label its sequencing requirement clearly.
- Automatically retrying `authorizeVoucherOutcome()`: rejected. Its one exact
  authorization-attempt guarantee is a fiscal safety boundary.
- Treating ISO and ARCA currency identifiers as aliases in one field: rejected.
  New builders use ISO codes; the exact WSFE input keeps the ARCA `currencyId`.
- Treating the service factories as uncallable: rejected. Structural custom
  implementations can call them. Their long-term public status is deferred.
- Fully typing the raw WSMTXCA authorization body: deferred until after v0.7.0.
- Refactoring the large WSFE and WSMTXCA service modules: deferred until
  contract fixtures exist and the v0.7.0 API is stable.
- Adding WSFEX, CAEA, or new FCE capabilities: outside this hardening program.
